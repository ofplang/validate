spec_version: "0.0"
processes:
  step: { kind: atomic, inputs: {}, outputs: {} }
  main:
    kind: composite
    inputs: {}
    outputs: {}
    body:
      nodes:
        - { id: s, process: step, bind: oops }
      returns: {}
entry: main
