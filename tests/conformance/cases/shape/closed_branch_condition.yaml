spec_version: "0.0"
features: [node_branch]
processes:
  a: { kind: atomic, inputs: {}, outputs: {} }
  main:
    kind: composite
    inputs:
      c: { type: Bool, phase: data }
    outputs: {}
    body:
      nodes:
        - id: b
          kind: branch
          condition: { from: inputs.c, bogus: 1 }
          then: { process: a }
          else: { process: a }
      returns: {}
entry: main
