spec_version: "0.0"
types:
  Cup: { domain: object, view: { v: { type: Float } } }
processes:
  main:
    kind: atomic
    inputs:
      cup: { type: Cup, phase: data }
    outputs:
      cup: { type: Cup, phase: data }
    objects:
      map: { outputs.cup: inputs.cup }
      bogus: {}
entry: main
