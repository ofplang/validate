spec_version: "0.0"
processes:
  main:
    kind: atomic
    inputs:
      x: { type: Int, phase: data }
    outputs: {}
    contracts:
      requires:
        - expr: "inputs.x.view > 0"
      bogus: []
entry: main
