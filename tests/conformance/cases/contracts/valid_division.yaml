spec_version: "0.0"
processes:
  main:
    kind: atomic
    inputs:
      x:
        type: Int
        phase: data
    outputs: {}
    contracts:
      requires:
        - expr: "inputs.x.view / 2 > 1"
        - expr: "1 / inputs.x.view > 1"
entry: main
